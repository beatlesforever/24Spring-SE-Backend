<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.sebackend.mapper.UsageRecordMapper">

    <select id="saveEndRecord" resultType="com.example.sebackend.entity.UsageRecord">
        select * from usage_record where room_id = #{roomId}
    </select>
</mapper>